


<script>
	let name = 'world';
</script>

<input bind:value={name}>

<h1>Hello {name}!</h1>







<!-- 일반적으로 Svelte의 데이터 흐름은 하향식입니다.
     상위 구성 요소는 하위 구성 요소에 props를 설정할 수 있고 
     구성 요소는 요소에 속성을 설정할 수 있지만 그 반대는 아닙니다.

때때로 그 규칙을 깨는 것이 유용합니다. 
이 구성 요소의 <input> 요소의 경우를 살펴보겠습니다. 
name 값을 event.target.value로 설정하는 on:input 이벤트 핸들러를 추가할 수 있지만 
약간... 상용구입니다. 우리가 보게 되겠지만, 다른 양식 요소에서는 더욱 악화됩니다.

대신 bind:value 지시문을 사용할 수 있습니다.

name 값을 변경하면 입력 값이 업데이트될 뿐만 아니라 
입력 값이 변경되면 이름도(변수의 밸류도) 업데이트됩니다.
-->